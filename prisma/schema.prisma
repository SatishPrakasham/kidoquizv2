generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model QRCode {
  id          String  @id @default(uuid())
  timestamp   BigInt
  isUsed      Boolean @default(false)
  qrImageData String  
}
